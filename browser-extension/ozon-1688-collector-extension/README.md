# 逐梦 ERP 采集端插件

这是 Ozon-1688 ERP 的浏览器插件版采集端。

## 当前版本

`0.1.0` 是连接预览版：

- 支持在插件里登录 ERP 账号
- 支持保持在线心跳
- ERP 网页端可以看到当前浏览器插件在线
- 暂不自动领取正式采集任务，避免预览版误领任务

## 安装测试

1. 打开 Chrome 或 Edge。
2. 地址栏输入 `chrome://extensions/`。
3. 打开开发者模式。
4. 如果下载的是 zip，请先解压，不要直接把 zip 拖进 Chrome。
5. 点击“加载已解压的扩展程序”。
6. 选择解压后包含 `manifest.json` 的文件夹。
7. 点击插件图标，输入 ERP 地址、账号和密码。

## 下一步

将现有 Node collector 的 Ozon 页面采集、1688 以图搜货、AI 审核和结果回传逻辑拆分迁移到插件中。
