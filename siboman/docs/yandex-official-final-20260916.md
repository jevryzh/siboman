# Yandex 官方分优化 · 复查报告（2026-09-16）

店铺 businessId `216994951` ｜ 全店 **1217** 个商品 ｜ 生成时间 2026-09-16 13:00 (CST)

## 一句话结论

全店官方分均值从 **55.36 → 74.79**（+19.43），**≥80 达标 455 个（37.4%）**；但**仍有 561 个商品处于平台重算中**（其中补图队列 501 个全部未结算），**本报告为中期数据，最终达标率需待重算收敛后确认**。

## 一、全店三段分布（1217 个商品）

| 阶段 | ≥80 | 60–79 | <60 | 均值 |
|---|---|---|---|---|
| ① 优化前 | 0 | 429 | 789 | **55.36** |
| ② 第一轮属性后（仅覆盖 773 个目标） | 0 | 676 | 97 | 69.18 |
| ③ 当前 | **455** | 662 | 101 | **74.79** |

> 注：② 只统计了第一轮后仍 <80 的 773 个目标商品，与 ①③ 的全量口径不可直接比较；同口径对比请看下方分组表。

## 二、分组明细（同口径：优化前 → 第二轮前 → 当前）

| 队列 | 目标数 | 已结算 | 重算中 | 已结算达标 | 均值 第二轮前 → 当前 |
|---|---|---|---|---|---|
| **补图队列**（首图/细节图/信息图） | 501 | 0 | **501** | 待重算 | 68.84 → 68.62（重算中，暂不可比） |
| **仅属性队列**（第二轮） | 263 | 207 | 56 | 8（3.9%） | 70.21 → **70.41**（+0.20） |
| 含视频建议的商品 | 718 | 203 | 515 | 8（3.9%） | 69.34 → 69.28 |
| **唯一目标合计** | **772** | 212 | 560 | 8 | 69.19 → 69.12 |

## 三、补图策略验证（试点 5 个商品）

`FIRST_PICTURE_SIZE` 建议已从**全部 5 个**商品的建议列表中消失，且 5 个商品分数均已大幅提升：

| offerId | ① 优化前 | ② 第一轮后 | ③ 当前 | FIRST_PICTURE_SIZE |
|---|---|---|---|---|
| jz-43c4a1-3989854024 | 24 | 63 | **71** | ✅ 已消除 |
| jz-74no4j-4373210704 | 25 | 62 | **78** | ✅ 已消除 |
| jz-2608156czu-5434366857 | 26 | 69 | **73** | ✅ 已消除 |
| jz-74no4j-4209730086 | 26 | — | **82**（已达标） | ✅ 已消除 |
| jz-2nf96y-3985003257 | 28 | 46 | **69** | ✅ 已消除 |

**验证结论**：首图「1:1 正方形 + ≥1500px」策略有效，故已推广到 501 个商品的批量补图（含首图增强 / 细节图 / 真实参数信息图）。该批量已全部提交成功（501/501），目前全部在平台重算中。

## 四、仍 <80 的原因归类（已结算的 205 个）

| 剩余原因 | 数量 | 占比 |
|---|---|---|
| **视频 + 属性** | **190** | 92.7% |
| 图片 + 视频 + 属性 | 5 | 2.4% |
| 视频 + 属性 + 描述 | 4 | 2.0% |
| 属性 | 4 | 2.0% |
| 无建议项（仅平台侧提示） | 2 | 1.0% |

**核心判断：瓶颈已从「属性/图片」转移到「缺视频」**。205 个已结算未达标商品中，**199 个（97%）带视频类建议**。视频维度按你的决定**未执行**，这是当前剩余差距的主要来源。

## 五、执行记录

| 项目 | 结果 |
|---|---|
| 第一轮属性优化 | 1217 个（全店） |
| 第二轮属性补全 | 516 个：成功 **506** / 失败 **10** |
| 补图批量（首图+细节图+信息图） | 501 个：成功 **501** / 失败 **0** |

**补图批量过程中修复的两个真实缺陷**（已重做受影响商品）：
1. 参数信息图排版缺陷 —— 内容仅占画布顶部约 45%、下方大片空白 → 改为按可用高度自适应字号并垂直居中，铺满画布；
2. 参数 JSON 转义缺陷 —— 参数值含双引号时经 Python 三引号字符串转义会损坏、导致 `json.loads` 报错 → 改为 base64 传参；
   附带修复：长俄文参数名原本硬截断 22 字符并溢出画布右边缘 → 改为按像素宽度智能省略。

## 六、结论与下一步

1. **属性维度已接近饱和**：第二轮属性补全（506 个商品）对已结算商品的均分贡献仅 **+0.20**，说明属性完整度不再是主要短板。
2. **图片维度待验证**：501 个补图商品全部仍在平台重算中，重算完成后才能确认图片策略的实际增益（试点已验证有效）。
3. **视频维度是剩余差距的主因**：718 个商品带视频类建议；若维持「不做视频」的决策，则达标率将受限，建议后续单独评估视频补全的投入产出。
4. **下一步**：等待平台重算收敛（预计数小时）后做最终复查，输出收敛后的稳定达标率。

## 附

- 仍 <80 商品清单（762 行，含 offerId / 优化前 / 第一轮后 / 当前 / 状态 / 原因 / 名称）：`docs/yandex-final-still-low-20260916.csv`
- 复查脚本：`/tmp/yk_final_review.mjs`（按 offerId 批量精确拉取，**不使用分页** —— 经诊断 `offer-cards` 的 `pageToken` 不生效，每页返回同一批 100 条）
