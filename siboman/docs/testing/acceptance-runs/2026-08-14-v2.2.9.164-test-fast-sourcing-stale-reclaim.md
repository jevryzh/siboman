# 验收记录：v2.2.9.164-test-fast-sourcing-stale-reclaim

- 日期：2026-08-14
- 目的：单品找货提速 + 稳定性（对齐生产旧版机制）
- `/api/version.version`：`v2.2.9.164-test-fast-sourcing-stale-reclaim`
- `/api/version.buildTime`：`2026-08-14T10:39:39.918Z`
- 插件 manifest version：`2.2.9.76`
- 插件 zip sha256：`ecde6c84c9896e0935ed2e5584354a8366aa02be6be9bd309e44287dd17f58c2`
- 备份目录：`/opt/ozon/backups/test-20260814-183916`
- 测试账号与店铺：未登录验证（页面交互项待用户验收）

## 本次改动

1. 插件 v2.2.9.76 `background.js`：
   - 找货任务默认 `skipOzonEnrichment=true`：跳过 Seller 后台复制商品富化链（seller bundle/OPI/类目解析/富文本），单行省 10-40s。
   - 找货任务默认 `fast1688=true`：1688 候选详情启用轻量浏览（3 次滚动 ~2-3.5s，替代 human-browse 剧场 5-13s），候选间停顿 2.5-6.5s → 1.2-3.8s。
   - `collectSku` / 1688 链路新增 `opts` 透传，非找货流程（collectSkus 消息，带 storeIds）行为不变。
2. 服务端 `server.js`：
   - `claimNextDbJob` 支持超时任务重新领取：claimed/running 且 90s（`WORKER_JOB_RECLAIM_MS`，下限 60s）未推进、无该 job 新鲜心跳、且非服务器收尾阶段（服务器 AI 审核/生成 Excel）的任务会被下一个轮询自动重新领取并从断点继续。
   - 新增环境变量 `WORKER_JOB_RECLAIM_MS`（默认 90s）。
   - `MIN_SINGLE_SOURCING_PLUGIN_VERSION` 提升到 `2.2.9.76`。
3. 版本一致性：manifest / background VERSION / content-bridge / StoreManagement / SourcingModule / BatchUpload 全部同步到 2.2.9.76，zip 已重打。

## 验证

- `npm run test:erp` 全绿（含 test_single_sourcing_v2、test_operations_modules、check-extension-release）。
- 部署后 curl 检查：version、manifest（2.2.9.76）、zip sha256 与服务端文件一致。
- 服务端代码 grep 确认新逻辑已生效。

## 待用户验收

- 页面交互：登录、店铺切换、单品找货发起任务（需更新插件到 2.2.9.76：扩展程序页点重新加载，或重新下载 zip）。
- 性能对比：同批量任务每行耗时是否从 ~2 分钟降到 ~1 分钟；插件掉线后任务是否 90s 内自动续跑。
- 失败项截图/录屏：无。

## 回滚

- 备份：`/opt/ozon/backups/test-20260814-183916/app-test`
- 命令：`rsync -a --delete /opt/ozon/backups/test-20260814-183916/app-test/ /opt/ozon/app-test/ && systemctl restart ozon-app-test`
